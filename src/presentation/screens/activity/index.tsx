import React from 'react';
import Activity from './activity';

const ActivityPresentation: React.FC = () => {
  return <Activity myPlans={[]} onPressMore={() => {}} />;
};

export default ActivityPresentation;
